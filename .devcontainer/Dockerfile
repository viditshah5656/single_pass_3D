# Reserved for project-specific native dependencies.
# The default Codespaces image is intentionally used by devcontainer.json so the
# environment remains fast to rebuild. Native OpenMVS can be layered here later.
FROM mcr.microsoft.com/devcontainers/python:1-3.11-bookworm
